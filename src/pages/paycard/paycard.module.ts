import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
// import { PaycardPage } from './paycard';

@NgModule({
    declarations: [
        // PaycardPage,
    ],
    imports: [
        // IonicPageModule.forChild(PaycardPage),
    ],
})
export class PaycardPageModule { }
